@model IEnumerable<CatalogoDDD.MVC.ViewModels.AnuncioViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.NomeFantasia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Imagem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.NomeFantasia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Imagem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnuncioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnuncioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnuncioId })
        </td>
    </tr>
}

</table>
